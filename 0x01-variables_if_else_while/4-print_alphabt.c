#include <stdio.h>

/**
 * main - Entry Point
 * @void: no parameter
 *
 * Description: all but q and e
 * Return: 0 if successful
 */

int main(void)
{
	char strt = 'a';
	char lst = 'z';

	while (strt <= lst)
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);
		}
		c++;
	}

	putchar('\n');
	return (0);
}
