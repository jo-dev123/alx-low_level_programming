#include "main.h"

/**
 * _isupper - Entry Point
 * @c: stores letter for checking
 *
 * Description: find uppercases
 * Return: 0 if successful
 */

int _isupper(int c)
{
	if (c >= 'A' && c <='Z')
		return (1);
	else
		return (0)
}
